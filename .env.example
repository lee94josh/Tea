# PostgreSQL connection string
# Local dev (docker-compose):
DATABASE_URL="postgresql://tea:tea@localhost:5432/tea"

# Railway (automatically injected — don't set this manually on Railway):
# DATABASE_URL="postgresql://..."
